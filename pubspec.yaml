name: health_insurance_app
description: Complete Health Insurance Mobile App with CareConnect Services - Bupa Arabia Style
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # State Management
  provider: ^6.1.1

  # Navigation
  go_router: ^13.0.0

  # Localization
  intl: ^0.18.1
  flutter_localizations:
    sdk: flutter

  # Storage
  shared_preferences: ^2.2.2
  # Removed for web compatibility: flutter_secure_storage (mobile-only)

  # Authentication
  # Removed for web compatibility: local_auth (mobile-only biometric)
  pin_code_fields: ^8.0.1

  # UI Components
  flutter_svg: ^2.0.9
  cached_network_image: ^3.3.0
  shimmer: ^3.0.0
  table_calendar: ^3.0.9
  qr_flutter: ^4.1.0

  # File Handling - Removed for web compatibility
  # image_picker and file_picker don't work properly on web
  # image_picker: ^1.0.5
  # file_picker: ^6.1.1

  # Utils
  uuid: ^4.2.2
  equatable: ^2.0.5

  # Icons
  cupertino_icons: ^1.0.6
  font_awesome_flutter: ^10.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

  # Testing
  mockito: ^5.4.4
  build_runner: ^2.4.7
  test: ^1.24.9

flutter:
  uses-material-design: true

  # Removed generate: true as we're using custom AppLocalizations
  # Comment out empty assets for now
  # assets:
  #   - assets/images/
  #   - assets/icons/
